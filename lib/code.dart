import 'package:flutter/material.dart';

class Codeee extends StatelessWidget {
  const Codeee({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text(
        "zama janan waya kana sanga e tata salaam di aur kha",
        style: TextStyle(color: Colors.blue),
      ),
    );
  }
}